; ASG S-Expression Example: Factorial (Iterative)
; Вычисление факториала через цикл
; (Рекурсивные функции пока в разработке)

(let n 5)
(let result 1)
(let i 1)

(while (<= i n)
  (do
    (set result (* result i))
    (set i (+ i 1))))

result  ; 120 (5!)
